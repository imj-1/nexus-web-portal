import { ShortenPrefixPipe } from './shorten-prefix.pipe';

describe('ShortenPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
